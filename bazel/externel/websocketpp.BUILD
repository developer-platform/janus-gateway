cc_library(
    name = "websocketpp-lib",
    visibility = ["//visibility:public"],
    # includes = ["include"],
    hdrs = glob(["websocketpp/**/*.hpp"]),
    # srcs = glob(["src/**/*.cpp", "src/**/*.h"]),
)